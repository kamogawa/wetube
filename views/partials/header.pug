hader.header
    div.header__column
        a(href=routes.home)
            i.fab.fa-youtube
    div.headCer__column
        form(action=routes.search, method="get")
            input(type="text", placeholder="Header Seach", name="term")
    div.headCer__column
        ul
            li
                a(href=routes.join) Join
            li
                a(href=routes.login) Log In